/* eslint-disable no-console */
import { generatePhotosArray } from './photos.js';

const photos = generatePhotosArray();
console.log(photos);

// Здесь можно добавить дополнительный код для работы с фотографиями и отображения их на странице
